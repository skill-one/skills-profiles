{
  "type": "object",
  "additionalProperties": false,
  "required": ["domain", "reason"],
  "properties": {
    "domain": {
      "type": "array",
      "description": "Category tags ordered by fit, the primary first; usually exactly one",
      "items": {
        "type": "string",
        "enum": [
          "development", "testing", "data-analysis", "devops-security", "office-productivity",
          "content-creation", "design-media", "knowledge-management", "business-ops",
          "finance-payment", "education", "lifestyle", "other"
        ]
      },
      "minItems": 1,
      "maxItems": 3
    },
    "reason": {
      "type": "string",
      "description": "The justification, ONE short sentence"
    }
  }
}
