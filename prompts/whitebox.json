{
  "type": "object",
  "additionalProperties": false,
  "required": ["execution_flow", "mechanisms"],
  "properties": {
    "execution_flow": {
      "type": "array",
      "description": "主路径 (happy path) 执行流程, 按顺序 3~5 步",
      "items": {"type": "string"}
    },
    "mechanisms": {
      "type": "array",
      "description": "关键实现机制, 2~3 条; 依赖的外部工具/库/模型写在此处",
      "items": {"type": "string"}
    }
  }
}
