{
  "type": "object",
  "additionalProperties": false,
  "required": ["function", "input_output"],
  "properties": {
    "function": {
      "type": "string",
      "description": "这个 skill 是做什么的, 一句话"
    },
    "input_output": {
      "type": "array",
      "description": "典型「输入 → 输出」对照, 3~5 条",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["input", "output"],
        "properties": {
          "input": {
            "type": "string",
            "description": "用户实际会给出的输入, 如文件路径、URL、一段文本"
          },
          "output": {
            "type": "string",
            "description": "用户实际会拿到的输出, 如生成的文件、报告、代码"
          }
        }
      }
    }
  }
}
