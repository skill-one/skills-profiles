启动一个 **后台智能体** 来开展研究，这样你可以一边工作一边等待它读取。

它的任务：

1. 针对 **原始资料** 调查该问题（官方文档、源代码、规格说明、第一方 API），而不是对其的二手概述。将每一项声明追溯至其所属的源头。
2. 将研究结果写入一个单独的 Markdown 文件，并为每条声明标注来源。
3. 将其保存到仓库中已有的此类笔记存放处；遵循现有约定，若没有，则将其存放在合理位置并说明位置。
