编写当前对话的交接摘要，以便新的智能体能够继续工作。不要保存它，而是启动一个以该摘要作为提示词种子的后台智能体：`claude --bg --name "<descriptive name>" "<handoff summary)"`。该智能体从当前工作目录启动，并立即返回；用户通过 `claude agents` 对其进行管理。

始终传入 `-n`/`--name` 参数，并使用描述性的名称（例如 `--name "Fix login bug"`）；它设置了任务列表、会话选择器和终端标题中显示的名称。

在摘要中包含一个"建议技能"部分，指明下一个智能体应调用 Skill 工具调用的哪些技能。

不要重复其他产物（规格、计划、ADR、问题、提交、差异）中已捕获的内容，而是通过路径或 URL 引用它们。

由于摘要将成为智能体的提示词，请删除任何敏感信息，例如 API 密钥、密码或个人可识别信息。

如果用户传入了参数，将其视为下一个会话的聚焦内容描述，并据此调整摘要。
