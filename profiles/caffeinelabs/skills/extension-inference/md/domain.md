# extension-inference (`caffeinelabs/skills/extension-inference`)

## domain

### domain

- development

### reason

Recipe for integrating an LLM API into a Motoko/Caffeine backend (deps, code, config).
