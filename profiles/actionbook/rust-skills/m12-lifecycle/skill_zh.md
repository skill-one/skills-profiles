# 资源生命周期

> **第 2 层：设计选择**

## 核心问题

**资源应该在何时创建、使用和清理？**

在实现生命周期之前：
- 资源的作用域是什么？
- 谁负责清理工作？
- 出现错误时会发生什么？

---

## 生命周期模式 → 实现

| 模式 | 何时 | 实现 |
|------|------|------|
| RAII | 自动清理 | `Drop` 特性 |
| 懒加载初始化 | 延迟创建 | `OnceLock`, `LazyLock` |
| 连接池 | 重用昂贵资源 | `r2d2`, `deadpool` |
| 守护者 | 作用域内访问 | `MutexGuard` 模式 |
| 作用域 | 事务边界 | 自定义结构体 + Drop |

---

## 思考提示

在设计生命周期之前：

1. **资源成本是什么？**
   - 便宜 → 每次使用时创建
   - 昂贵 → 连接池或缓存
   - 全局 → 懒加载单例

2. **作用域是什么？**
   - 函数局部 → 栈分配
   - 请求作用域 → 传递或提取
   - 应用程序范围 → 静态或 Arc

3. **关于错误？**
   - 必须清理 → Drop
   - 清理是可选的 → 显式关闭
   - 清理可能失败 → 关闭的 Result

---

## 向上追溯 ↑

到领域约束（第 3 层）：

```
"如何管理数据库连接？"
    ↑ 询问：连接成本是多少？
    ↑ 检查：domain-* (延迟要求)
    ↑ 检查：基础设施 (连接限制)
```

| 问题 | 追溯到 | 询问 |
|------|--------|------|
| 连接池 | domain-* | 可接受的延迟是多少？ |
| 资源限制 | domain-* | 基础设施约束是什么？ |
| 事务作用域 | domain-* | 什么必须原子化？ |

---

## 向下追溯 ↓

到实现（第 1 层）：

```
"需要自动清理"
    ↓ m02-resource: 实现 Drop
    ↓ m01-所有权: 清理所有者

"需要懒加载初始化"
    ↓ m03-可变性: OnceLock 用于线程安全
    ↓ m07-并发: LazyLock 用于同步

"需要连接池"
    ↓ m07-并发: 线程安全池
    ↓ m02-resource: Arc 用于共享
```

---

## 快速参考

| 模式 | 类型 | 用例 |
|------|------|------|
| RAII | `Drop` 特性 | 作用域退出时自动清理 |
| 懒加载初始化 | `OnceLock`, `LazyLock` | 延迟初始化 |
| 连接池 | `r2d2`, `deadpool` | 连接重用 |
| 守护者 | `MutexGuard` | 作用域内锁释放 |
| 作用域 | 自定义结构体 | 事务边界 |

## 生命周期事件

| 事件 | Rust 机制 |
|------|----------|
| 创建 | `new()`, `Default` |
| 懒加载初始化 | `OnceLock::get_or_init` |
| 使用 | `&self`, `&mut self` |
| 清理 | `Drop::drop()` |

## 模式模板

### RAII 守护者

```rust
struct FileGuard {
    path: PathBuf,
    _handle: File,
}

impl Drop for FileGuard {
    fn drop(&mut self) {
        // 清理：删除临时文件
        let _ = std::fs::remove_file(&self.path);
    }
}
```

### 懒加载单例

```rust
use std::sync::OnceLock;

static CONFIG: OnceLock<Config> = OnceLock::new();

fn get_config() -> &'static Config {
    CONFIG.get_or_init(|| {
        Config::load().expect("config required")
    })
}
```

---

## 常见错误

| 错误 | 原因 | 修复 |
|------|------|------|
| 资源泄漏 | 忘记 Drop | 实现 Drop 或 RAII 包装器 |
| 双重释放 | 手动内存 | 让 Rust 处理 |
| 使用后释放 | 悬空引用 | 检查生命周期 |
| E0509 从 Drop 中移动 | 移动所属字段 | `Option::take()` |
| 连接池耗尽 | 未返回 | 确保 Drop 返回 |

---

## 反模式

| 反模式 | 为什么不好 | 更好 |
|--------|------------|------|
| 手动清理 | 容易忘记 | RAII/Drop |
| `lazy_static!` | 外部依赖 | `std::sync::OnceLock` |
| 全局可变状态 | 线程不安全 | `OnceLock` 或适当同步 |
| 忘记关闭 | 资源泄漏 | Drop 实现 |

---

## 相关技能

| 何时 | 查看 |
|------|------|
| 智能指针 | m02-resource |
| 线程安全初始化 | m07-并发 |
| 领域作用域 | m09-领域 |
| 清理中的错误 | m06-错误处理 |
