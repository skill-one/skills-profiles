# 修复代码风格和格式问题

## 说明

1. 运行 `yarn prettier` 来修复格式问题
2. 运行 `yarn linc` 来检查剩余的代码风格问题
3. 报告需要手动修复的任何剩余问题

## 常见错误

- **在错误的文件上运行 prettier** - `yarn prettier` 只格式化已更改的文件
- **忽略 linc 错误** - 这些错误会导致 CI 失败，在提交前需要修复它们
