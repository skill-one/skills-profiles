# deploying-ui-bundle (`forcedotcom/sf-skills/deploying-ui-bundle`)

## domain

### domain

- devops-security

### reason

The skill orchestrates the full deployment-and-release sequence to a Salesforce org (auth, deploy, configure, post-deploy steps).
