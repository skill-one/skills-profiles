# platform-metadata-retrieve (`forcedotcom/sf-skills/platform-metadata-retrieve`)

## domain

### domain

- development

### reason

CLI tool for pulling Salesforce metadata into a local dev project via `sf` command.
