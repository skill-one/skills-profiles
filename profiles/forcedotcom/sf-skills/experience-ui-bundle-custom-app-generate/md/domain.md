# experience-ui-bundle-custom-app-generate (`forcedotcom/sf-skills/experience-ui-bundle-custom-app-generate`)

## domain

### domain

- development

### reason

Generates Salesforce CustomApplication metadata XML and validates deployment via CLI — a developer configuration workflow.
