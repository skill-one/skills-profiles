# platform-custom-object-generate (`forcedotcom/sf-skills/platform-custom-object-generate`)

## domain

### domain

- development

### reason

Generates and validates Salesforce metadata XML files for deployment via the Metadata API.
