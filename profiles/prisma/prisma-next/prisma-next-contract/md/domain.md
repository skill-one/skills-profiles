# prisma-next-contract (`prisma/prisma-next/prisma-next-contract`)

## domain

### domain

- development

### reason

Authoring database schemas via an ORM (models, fields, relations) and running CLI emit commands is core development work.
