启动一个 **background agent** 进行研究，这样你可以在它阅读时继续工作。

它的工作：

1. 对照 **primary sources** 调研问题：官方文档、源代码、规范、第一方 API，而不是二手解读。每个 claim 都要追溯到拥有该事实的 source。
2. 将 findings 写入一个 Markdown 文件，并为每个 claim 标注 source。
3. 保存到 repo 既有的 notes 位置；匹配现有 convention。如果没有 convention，就放在合理位置并说明路径。
