# 并行插件设置

## 安装 CLI

请参考 https://docs.parallel.ai/integrations/cli 了解支持的安装方法（pipx、Homebrew、npm、原生二进制文件）。引导用户完成他们选择的安装方式。

## 认证

```bash
parallel-cli login
```

## 验证

```bash
parallel-cli auth
```

如果找不到 `parallel-cli`，请将 `~/.local/bin` 添加到 PATH 环境变量中。

## 后续更新

```bash
parallel-cli update
```
