# 迁移和代码演进指令生成器

## 配置变量

```
${MIGRATION_TYPE="框架版本|架构重构|技术迁移|依赖更新|模式变更"}
<!-- 迁移或演进的类型 -->

${SOURCE_REFERENCE="分支|提交|标签"}
<!-- 源参考点（之前的状态） -->

${TARGET_REFERENCE="分支|提交|标签"}  
<!-- 目标参考点（之后的状态） -->

${ANALYSIS_SCOPE="整个项目|特定文件夹|仅修改的文件"}
<!-- 分析范围 -->

${CHANGE_FOCUS="破坏性变更|新规范|过时模式|API变更|配置"}
<!-- 变更的主要方面 -->

${AUTOMATION_LEVEL="保守|平衡|激进"}
<!-- Copilot建议的自动化级别 -->

${GENERATE_EXAMPLES="true|false"}
<!-- 包含转换示例 -->

${VALIDATION_REQUIRED="true|false"}
<!-- 应用前需要验证 -->
```

## 生成的提示

```
"分析两个项目状态之间的代码演进，为GitHub Copilot生成精确的迁移指令。这些指令将指导Copilot在未来的修改中自动应用相同的转换模式。遵循以下方法：

### 第一阶段：比较状态分析

#### 结构变更检测
- 比较${SOURCE_REFERENCE}和${TARGET_REFERENCE}之间的文件夹结构
- 识别移动、重命名或删除的文件
- 分析配置文件的变化
- 记录新的依赖项和删除的依赖项

#### 代码转换分析
${MIGRATION_TYPE == "框架版本" ? 
  "- 识别框架版本之间的API变更
   - 分析使用的新功能
   - 记录过时的方法/属性
   - 注意语法或约定变更" : ""}

${MIGRATION_TYPE == "架构重构" ? 
  "- 分析架构模式的变化
   - 识别引入的新抽象
   - 记录责任重组
   - 注意数据流变化" : ""}

${MIGRATION_TYPE == "技术迁移" ? 
  "- 分析用另一种技术替换一种技术
   - 识别功能等价性
   - 记录API和语法变更
   - 注意新的依赖项和配置" : ""}

#### 转换模式提取
- 识别应用的重复转换
- 分析从旧格式到新格式的转换规则
- 记录例外情况和特殊情况
- 创建前后对应矩阵

### 第二阶段：迁移指令生成

创建一个`.github/copilot-migration-instructions.md`文件，具有以下结构：

\`\`\`markdown
# GitHub Copilot迁移指令

## 迁移上下文
- **类型**: ${MIGRATION_TYPE}
- **从**: ${SOURCE_REFERENCE} 
- **到**: ${TARGET_REFERENCE}
- **日期**: [GENERATION_DATE]
- **范围**: ${ANALYSIS_SCOPE}

## 自动转换规则

### 1. 强制转换
${AUTOMATION_LEVEL != "保守" ? 
  "[AUTOMATIC_TRANSFORMATION_RULES]
   - **旧模式**: [OLD_CODE]
   - **新模式**: [NEW_CODE]
   - **触发条件**: 何时检测此模式
   - **操作**: 应用自动转换" : ""}

### 2. 需要验证的转换
${VALIDATION_REQUIRED == "true" ? 
  "[TRANSFORMATIONS_WITH_VALIDATION]
   - **检测到的模式**: [描述]
   - **建议转换**: [新方法]
   - **需要验证**: [验证标准]
   - **替代方案**: [替代选项]" : ""}

### 3. API对应关系
${CHANGE_FOCUS == "API变更" || MIGRATION_TYPE == "框架版本" ? 
  "[API_CORRESPONDENCE_TABLE]
   | 旧API   | 新API   | 备注     | 示例        |
   | ------- | ------- | ------- | ---------- |
   | [OLD_API] | [NEW_API] | [变化] | [代码示例] | " : ""} |

### 4. 新模式采用
[DETECTED_EMERGING_PATTERNS]
- **模式**: [模式名称]
- **使用场景**: [何时使用] 
- **实现方式**: [如何实现]
- **优势**: [优点]

### 5. 避免过时模式
[DETECTED_OBSOLETE_PATTERNS]
- **过时模式**: [旧模式]
- **避免原因**: [原因]
- **替代方案**: [新模式]
- **迁移步骤**: [转换步骤]

## 文件类型特定指令

${GENERATE_EXAMPLES == "true" ? 
  "### 配置文件
   [CONFIG_TRANSFORMATION_EXAMPLES]
   
   ### 主要源文件
   [SOURCE_TRANSFORMATION_EXAMPLES]
   
   ### 测试文件
   [TEST_TRANSFORMATION_EXAMPLES]" : ""}

## 验证和安全

### 自动控制点
- 每次转换后需要执行的验证
- 需要运行的测试以验证变更
- 需要监控的性能指标
- 需要执行的兼容性检查

### 手动升级
需要人工干预的情况：
- [复杂情况列表]
- [架构决策]
- [业务影响]

## 迁移监控

### 跟踪指标
- 自动迁移的代码百分比
- 需要手动验证的数量
- 自动转换的错误率
- 每个文件的平均迁移时间

### 错误报告
如何向Copilot报告不正确的转换：
- 反馈模式以改进规则
- 记录例外情况
- 对指令进行调整

\`\`\`

### 第三阶段：上下文示例生成

${GENERATE_EXAMPLES == "true" ? 
  "#### 转换示例
   对于每个识别到的模式，生成：
   
   \`\`\`
   // 之前 (${SOURCE_REFERENCE})
   [OLD_CODE_EXAMPLE]
   
   // 之后 (${TARGET_REFERENCE}) 
   [NEW_CODE_EXAMPLE]
   
   // COPILOT指令
   当你看到此模式[TRIGGER]时，按照以下步骤将其转换为[NEW_PATTERN]: [步骤]
   \`\`\`" : ""}

### 第四阶段：验证和优化

#### 指令测试
- 在测试代码上应用指令
- 验证转换一致性
- 根据结果调整规则
- 记录例外情况和边缘情况

#### 迭代优化  
${AUTOMATION_LEVEL == "激进" ? 
  "- 精炼规则以最大化自动化
   - 减少检测中的误报
   - 提高转换准确性
   - 记录学到的经验" : ""}

### 最终结果

使GitHub Copilot能够：
1. **自动应用** 在未来修改中相同的转换
2. **保持一致性** 与新采用的规范  
3. **避免过时模式** 通过自动提出替代方案
4. **加速未来迁移** 通过利用已获得的经验
5. **减少错误** 通过自动化重复转换

这些指令将Copilot转变为一个智能迁移助手，能够一致且可靠地复制您的技术演进决策。
"
```

## 典型用例

### 框架版本迁移
非常适合记录从Angular 14到Angular 17、React类组件到Hooks或.NET Framework到.NET Core的过渡。自动识别破坏性变更并生成相应的转换规则。

### 技术栈演进  
在完全替换技术时必不可少：jQuery到React、REST到GraphQL、SQL到NoSQL。创建一个全面的迁移指南，包含模式映射。

### 架构重构
非常适合大型重构，如单体到微服务、MVC到干净架构或组件到可组合架构。保留架构知识，以便未来进行类似的转换。

### 设计模式现代化
适用于采用新模式：仓库模式、依赖注入、观察者到响应式编程。记录其合理性和实现差异。

## 独特优势

### 🧠 **人工智能增强**
与传统的迁移文档不同，这些指令使GitHub Copilot能够在未来的代码修改中自动复制您的技术演进决策。

### 🔄 **知识资本化**  
将特定项目的经验转化为可重用的规则，避免迁移专业知识的丢失，并加速未来的类似迁移。

### 🎯 **上下文感知精确**
而不是通用建议，生成针对您特定代码库的指令，包含来自您项目演进的实时前后示例。

### ⚡ **自动化一致性**
确保新代码添加自动遵循新规范，防止架构回归，并保持代码演进的连贯性。
