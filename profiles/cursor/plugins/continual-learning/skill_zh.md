# 持续学习

通过将内存更新流程委托给一个子代理来保持 `AGENTS.md` 的最新状态。

## 触发条件

当用户要求挖掘先前的聊天记录、维护 `AGENTS.md` 或运行持续学习循环时使用。

## 工作流程

1. 调用 `agents-memory-updater`。
2. 返回更新器结果。

## 安全约束

- 保持父技能仅进行编排。
- 在父流程中不要挖掘文本记录或编辑文件。
- 不要绕过子代理。
