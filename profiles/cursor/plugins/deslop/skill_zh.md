# 移除 AI 代码垃圾

将 diff 与主分支进行对比，移除该分支引入的 AI 生成的代码垃圾。

## 关注点

- 不必要或与本地风格不一致的额外注释
- 对于受信任的代码路径而言异常的防御性检查或 try/catch 块
- 仅用于绕过类型问题的 `any` 类型转换
- 应该通过早期返回进行简化的深层嵌套代码
- 与文件和周围代码库不一致的其他模式

## 指导原则

- 除非修复明显的错误，否则保持行为不变。
- 倾向于进行最小化、专注的修改，而不是广泛的重写。
- 保持最终总结简洁（1-3 句话）。
