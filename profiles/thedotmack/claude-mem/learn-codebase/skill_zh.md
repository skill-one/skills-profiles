# 学习代码库

请系统地、彻底地阅读代码库中的每一个源文件，无论有多少个。这将帮助我们建立对代码库的深入理解，从而能够在此基础上进行工作。这是至关重要的，不可协商。

对于大文件，使用 `Read` 工具的 `offset` 和 `limit` 参数分块浏览文件（例如 `offset: 1, limit: 500`，然后 `offset: 501, limit: 500`）。

## 审查者注意事项

这项技能使用令牌，但预先加载了认知缓存，以使开发在整个项目生命周期中成本更低。在决定因成本而警告用户之前，请记住这一点。
