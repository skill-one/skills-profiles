指导用户完成他们的第一个完整的 OpenSpec 工作流周期。这是一个教学体验——你将在他们的代码库中做实际工作，同时解释每个步骤。

**商店选择：** 如果用户命名了一个商店（商店是一个独立于 OpenSpec 仓库注册在此机器上的 OpenSpec 仓库）或工作存在于其中，运行 `openspec store list --json` 来发现已注册的商店 ID，然后在读取或写入规范和更改的命令（`new change`、`status`、`instructions`、`list`、`show`、`validate`、`archive`、`doctor`、`context`、`schemas`、`view`）上传递 `--store <id>`。一旦选择，将 `--store <id>` 视为工作流其余部分的粘性。下面所有未限定示例的命令都是简称：在运行它之前，附加该标志。例如，运行 `openspec status --change "<name>" --json --store "<id>"`，而不是下面显示的无限定形式。其他命令不接受该标志。命令打印的提示已经包含该标志；在后续操作中保持它。没有商店，命令作用于最近的本地 `openspec/` 根目录。

**项目检查：** 这些步骤期望一个已经使用 OpenSpec 的项目。在第一个写入任何内容的步骤（`new change`、`archive`、`sync specs` 或编写工件文件）之前，确认项目有一个根：运行 `openspec list --json`（当选择商店时使用 `--store <id>`，因为商店是根）并读取 `root`。根对象表示项目已设置。`"root": null` 表示它没有设置——这里没有 `openspec/` 目录，并且写入（例如 `openspec new change`）会作为副作用创建一个。该命令退出非零，这是答案而不是损坏的 CLI，因此请读取 JSON 而不是重试或绕过它。

一个 `"root": null` 不是关于设置的：当 `status` 错误消息以 `Declared in` 或 `Invalid store declaration in` 开头并命名此项目的 `openspec/config.yaml`（或 `config.yml`）时，该项目确实通过它声明的商店使用 OpenSpec，但此机器无法解析（商店未注册，或 `store:` 行格式不正确）。不要将其视为未初始化并跳过下面的分支：在写入之前停止并显示该错误的 `message` 和 `fix`。

否则，没有根，接下来会发生什么取决于此工作流是如何被触发的：

- **自动选择**：你选择了这个工作流，而没有用户命名 OpenSpec、命名此技能或运行其斜杠命令。停止使用 OpenSpec 并像没有安装 OpenSpec 时一样正常回答请求。不要让他们设置任何内容，也不要提及 OpenSpec 设置。
- **明确的 OpenSpec 请求**：用户命名了 OpenSpec、命名了此技能或运行了其斜杠命令。在写入之前停止并询问如何进行：设置此项目（`openspec init`）、目标他们已经拥有的商店（`--store <id>`）或为此请求不使用 OpenSpec 继续进行。等待他们的回答。

在这两个分支中，永远不要作为副作用创建根：直到用户要求它之前，不要运行 `openspec init`，不要手动创建 `openspec/` 文件，也不要让命令创建它。

---

## 预检查

开始之前，检查 OpenSpec CLI 是否已安装：

```bash
# Unix/macOS
openspec --version 2>&1 || echo "CLI_NOT_INSTALLED"
# Windows (PowerShell)
# if (Get-Command openspec -ErrorAction SilentlyContinue) { openspec --version } else { echo "CLI_NOT_INSTALLED" }
```

**如果 CLI 未安装：**
> OpenSpec CLI 未安装。先安装它，然后回到 `/openspec-onboard`。

如果未安装，则停止在此。

---

## 第一阶段：欢迎

显示：

```
## 欢迎使用 OpenSpec！

我将引导你完成一个完整的变化周期——从想法到实现——使用你代码库中的实际任务。在这个过程中，你将通过实践来学习工作流。

**我们将做什么：**
1. 在你的代码库中选择一个小的实际任务
2. 简要探索问题
3. 创建一个变化（我们工作的容器）
4. 构建工件：提议 → 规范 → 设计 → 任务
5. 实现任务
6. 归档已完成的变化

**时间：** 约 15-20 分钟

让我们开始寻找一些可以工作的东西。
```

---

## 第二阶段：任务选择

### 代码库分析

扫描代码库以寻找小的改进机会。寻找：

1. **TODO/FIXME 评论** - 在代码文件中搜索 `TODO`、`FIXME`、`HACK`、`XXX`
2. **缺少错误处理** - 吞噬错误的 `catch` 块、没有 try-catch 的风险操作
3. **没有测试的函数** - 交叉引用 `src/` 与测试目录
4. **类型问题** - TypeScript 文件中的 `any` 类型（`: any`、`as any`）
5. **调试工件** - 非调试代码中的 `console.log`、`console.debug`、`debugger` 语句
6. **缺少验证** - 没有验证的用户输入处理程序

还要检查最近的 git 活动情况：
```bash
# Unix/macOS
git log --oneline -10 2>/dev/null || echo "No git history"
# Windows (PowerShell)
# git log --oneline -10 2>$null; if ($LASTEXITCODE -ne 0) { echo "No git history" }
```

### 提出建议

根据你的分析，提出 3-4 个具体建议：

```
## 任务建议

根据扫描你的代码库，这里有一些好的入门任务：

**1. [最有希望的任务]**
   位置：`src/path/to/file.ts:42`
   范围：约 1-2 个文件，约 20-30 行
   为什么它很好：[简要原因]

**2. [第二个任务]**
   位置：`src/another/file.ts`
   范围：约 1 个文件，约 15 行
   为什么它很好：[简要原因]

**3. [第三个任务]**
   位置：[位置]
   范围：[估计]
   为什么它很好：[简要原因]

**4. 其他？**
   告诉我你想做什么。

哪个任务让你感兴趣？(选择一个数字或描述你自己的)
```

**如果没有找到：** 回退到询问用户想构建什么：
> 我没有在你的代码库中找到明显的快速赢。你有什么小东西想添加或修复？

### 范围护栏

如果用户选择或描述的东西太大（主要功能、多天工作）：

```
这是一个有价值的任务，但它可能比你第一次运行 OpenSpec 时理想的大。

为了学习工作流，小一些更好——这样你可以在不陷入实现细节的情况下看到完整周期。

**选项：**
1. **切小一些** - [他们的任务] 的最小有用部分是什么？也许是 [具体切片]？
2. **选择其他** - 其他建议，或不同的一个小任务？
3. **无论如何做** - 如果你真的想处理这个，我们可以。只是知道它会更长。

你更喜欢什么？
```

如果用户坚持，允许他们覆盖——这是一个软护栏。

---

## 第三阶段：探索演示

一旦选择了一个任务，简要演示探索模式：

```
在创建变化之前，让我快速向你展示 **探索模式**——它是你在承诺方向之前思考问题的方式。
```

花 1-2 分钟调查相关代码：
- 阅读相关的文件
- 如果有助于理解，画一个快速的 ASCII 图表
- 记录任何考虑事项

```
## 快速探索

[你的简要分析——你发现了什么，任何考虑事项]

┌─────────────────────────────────────────┐
│   [可选：如果有助于理解，ASCII 图表]  │
└─────────────────────────────────────────┘

探索模式 (`/openspec-explore`) 是用于这种思考的方式——在实现之前进行调查。当你需要思考一个问题时，你可以随时使用它。

现在让我们创建一个变化来保存我们的工作。
```

**暂停** - 等待用户确认后再继续。

---

## 第四阶段：创建变化

**解释：**
```
## 创建变化

OpenSpec 中的“变化”是一个容器，用于围绕一个任务的所有思考和计划。它位于 `openspec status --change "<name>" --json` 报告的 `changeRoot`，并包含你的工件——提议、规范、设计、任务。

让我为我们的任务创建一个。
```

**做：** 使用派生的 kebab-case 名称创建变化：
```bash
openspec new change "<derived-name>"
```

**显示：**
```
创建：`<changeRoot from status JSON>`

文件夹结构：
```
<changeRoot>/
├── proposal.md    ← 为什么我们这样做（空，我们将填充它）
├── design.md      ← 我们将如何构建它（空）
├── specs/         ← 详细要求（空）
└── tasks.md       ← 实现清单（空）
```

现在让我们填充第一个工件——提议。
```

---

## 第五阶段：提议

**解释：**
```
## 提议

提议捕获了我们为什么做这个变化以及它的高级内容。它是这个工作的“电梯演讲”。

我将根据我们的任务起草一个。
```

**做：** 起草提议内容（不要保存）：

`<capability-path>` 是相对于 `specs/` 的规范目录（例如，
`user-auth` 或 `identity/user-auth`）。对于修改的现有能力，请使用确切的现有路径。对于新能力，请遵循项目的规范组织方式。

```
这是一个草案提议：

---

# 提议

## 为什么

[1-2 句话解释问题/机会]

## 变化内容

[要点列表，说明将会有什么不同]

## 能力

### 新能力
- `<capability-path>`：[简要描述]

### 修改的能力
<!-- 如果修改现有行为 -->
- `<existing-capability-path>`：[简要描述]

## 影响

- `src/path/to/file.ts`：[什么变化]
- [其他文件（如果适用）]

---

这个捕获了意图吗？我可以在保存之前调整。
```

**暂停** - 等待用户批准/反馈。

批准后，保存提议：
```bash
openspec instructions proposal --change "<name>" --json
```
然后将内容写入 `resolvedOutputPath` 从 `openspec instructions proposal --change "<name>" --json`。

```
提议已保存。这是你的“为什么”文档——你可以随时回来并根据理解的演变进行完善。

下一步：规范。
```

---

## 第六阶段：规范

**解释：**
```
## 规范

规范以精确、可测试的术语定义我们将要构建的内容。它们使用要求/场景格式，使预期行为一目了然。

对于像这样小的任务，我们可能只需要一个规范文件。
```

**做：** 确定规范文件应创建的位置：
```bash
openspec instructions specs --change "<name>" --json
# 使用 JSON 中的 resolvedOutputPath。如果它是 glob，请使用模式指令和变化的上下文选择具体的文件路径。
```

起草规范内容：

```
这是一个规范：

---

# 规范增量

## 添加的要求

### 要求：<名称>

<系统应该做什么的描述>

#### 场景：<场景名称>

- **当** <触发条件>
- **则** <预期结果>
- **并且** <如果需要，附加结果>

---

这种格式——WHEN/THEN/AND——使要求可测试。你可以把它们字面理解为测试用例。
```

保存到从 `resolvedOutputPath` 选择的具体文件路径。

---

## 第七阶段：设计

**解释：**
```
## 设计

设计捕获了我们如何构建它——技术决策、权衡、方法。

对于小的变化，这可能很简短。这很好——不是每个变化都需要深入的设计讨论。
```

**做：** 起草 design.md：

```
这是一个设计：

---

# 设计

## 背景

[关于当前状态的简要背景]

## 目标 / 非目标

**目标：**
- [我们试图实现什么]

**非目标：**
- [明确不在范围内的东西]

## 决策

### 决策 1：[关键决策]

[决策的解释和理由]

---

对于小任务，这捕获了关键决策，而不会过度设计。
```

保存到 `resolvedOutputPath` 从 `openspec instructions design --change "<name>" --json`。

---

## 第八阶段：任务

**解释：**
```
## 任务

最后，我们将工作分解为实施任务——驱动应用阶段的复选框。

这些应该是小而清晰的，并按逻辑顺序排列。每个组都包含其自己的测试和文档——最后一个组仅用于集成检查。
```

**做：** 根据规范和设计生成任务：

```
这里是实施任务：

---

# 任务

## 1. [类别或文件]

- [ ] 1.1 [具体任务] — 验证：[测试、命令、可观察行为或交付的工件]
- [ ] 1.2 [具体任务] — 验证：[测试、命令、可观察行为或交付的工件]

## 2. 集成验证

- [ ] 2.1 验证 [更广泛的集成或系统行为] 使用 [端到端测试或可观察结果]

---

每个复选框都成为应用阶段的一个工作单元。这个任务分解看起来对吗？
```

**暂停** - 等待用户批准/反馈。

批准后，保存到 `resolvedOutputPath` 从 `openspec instructions tasks --change "<name>" --json`。

然后询问：
> "任务已保存。准备好实现吗？"

**暂停** - 等待用户确认后再实现。

---

## 第九阶段：应用（实现）

**解释：**
```
## 实现

现在我们实现每个任务，边做边标记它们。我将宣布每个任务，并偶尔指出规范/设计如何指导方法。
```

**做：** 对于每个任务：

1. 宣布："正在处理任务 N：[描述]"
2. 在代码库中实现更改
3. 自然地引用规范/设计："规范说 X，所以我做 Y"
4. 在 tasks.md 中标记完成：`- [ ]` → `- [x]`
5. 简要状态："✓ 任务 N 完成"

保持叙述轻快——不要过度解释每一行代码。

所有任务完成后：

```
## 实现完成

所有任务完成：
- [x] 任务 1
- [x] 任务 2
- [x] ...

变化已实现！最后一步——让我们归档它。
```

---

## 第十阶段：归档

**解释：**
```
## 归档

当变化完成时，我们将其归档。归档路径由 `planningHome.changesDir` 和日期派生。

归档的变化成为你项目决策历史的一部分——你可以随时找到它们，以了解为什么某件事以某种方式构建。
```

**做：** 归档变化（`--yes` 回答确认提示，你无法从工具调用中回答）：
```bash
openspec archive "<name>" --yes
```

**显示：**
```
归档到：`<planningHome.changesDir>/archive/<target-name>/`（目标名称添加了今天的日期，除非名称已经以 `YYYY-MM-DD-` 前缀开头——那么它保持不变，不会添加第二个日期）

变化现在是你的项目历史的一部分。代码在你的代码库中，决策记录被保留。
```

---

## 第十一阶段：回顾和下一步

```
## 恭喜！

你刚刚完成了一个完整的 OpenSpec 周期：

1. **探索** - 思考了问题
2. **新建** - 创建了变化容器
3. **提议** - 捕获了“为什么”
4. **规范** - 详细定义了“什么”
5. **设计** - 决定了“如何”
6. **任务** - 将其分解为步骤
7. **应用** - 实现了工作
8. **归档** - 保存了记录

这个节奏同样适用于任何大小的变化——一个小修复或一个主要功能。

---

## 命令参考

**你安装的命令：**

 | 命令          | 它做什么                               |
 |------------------|--------------------------------------------|
 | `/openspec-propose` | 创建变化并生成所有工件                 |
 | `/openspec-explore` | 在工作之前/期间思考问题                |
 | `/openspec-apply-change`   | 从变化中实现任务                      |
 | `/openspec-archive-change` | 归档已完成的变化                     |
 | `/openspec-new-change`     | 开始一个新的变化，一次一个工件         |
 | `/openspec-continue-change` | 继续处理现有的变化                    |
 | `/openspec-ff-change`      | 快速前进：一次创建所有工件             |
 | `/openspec-verify-change`  | 验证实现是否匹配工件                  |

---

## 下一步是什么？

在你要实际构建的东西上尝试 `/openspec-propose`。你现在有了节奏！
```

---

## 优雅的退出处理

### 用户想中途停止

如果用户说他们需要停止，想暂停，或看起来不感兴趣：

```
没问题！你的变化保存在 `changeRoot` 中，由 `openspec status --change "<name>" --json` 报告。

稍后从我们离开的地方继续，`openspec status --change "<name>" --json` 显示变化的确切状态。
- `/openspec-continue-change <name>` - 继续创建工件
- `/openspec-apply-change <name>` - 跳到实现（如果任务存在）

工作不会丢失。准备好时随时回来。
```

优雅地退出，不要施加压力。

### 用户只想查看命令参考

如果用户说他们只想查看命令或跳过教程：

```
## OpenSpec 快速参考

**你安装的命令：**

 | 命令                  | 它做什么                               |
 |--------------------------|--------------------------------------------|
 | `/openspec-propose <name>`  | 创建变化并生成所有工件                 |
 | `/openspec-explore`         | 思考问题（不更改代码）                |
 | `/openspec-apply-change <name>`    | 实现任务                              |
 | `/openspec-archive-change <name>`  | 完成时归档                          |
 | `/openspec-new-change <name>`      | 开始一个新的变化，逐步进行           |
 | `/openspec-continue-change <name>` | 继续现有的变化                        |
 | `/openspec-ff-change <name>`       | 快速前进：一次所有工件               |
 | `/openspec-verify-change <name>`   | 验证实现                              |

尝试 `/openspec-propose` 开始你的第一个变化。
```

优雅地退出。

---

## 护栏

- **在关键转换时遵循 EXPLAIN → DO → SHOW → PAUSE 模式**（探索之后、提议草案之后、任务之后、归档之后）
- **在实现期间保持叙述轻快**——教而不讲
- **即使变化很小，也不要跳过阶段**——目标是教学工作流
- **在标记点暂停以获取确认**，但不要过度暂停
- **优雅地处理退出**——永远不要强迫用户继续
- **使用真实的代码库任务**——不要模拟或使用假示例
- **温和地调整范围**——引导向更小的任务，但尊重用户选择
