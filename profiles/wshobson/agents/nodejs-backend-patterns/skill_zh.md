# Node.js 后端模式

构建可扩展、可维护且适用于生产环境的 Node.js 后端应用程序的现代框架、架构模式和最佳实践的全面指南。

## 何时使用此技能

- 构建 REST API 或 GraphQL 服务器
- 使用 Node.js 创建微服务
- 实现身份验证和授权
- 设计可扩展的后端架构
- 设置中间件和错误处理
- 集成数据库（SQL 和 NoSQL）
- 使用 WebSockets 构建实时应用程序
- 实现后台任务处理

## 详细模式和实例

详细模式文档位于 `references/details.md`。当上层导航层级不足时，请阅读该文件。

## 最佳实践

1. **使用 TypeScript**：类型安全可防止运行时错误
2. **实现适当的错误处理**：使用自定义错误类
3. **验证输入**：使用 Zod 或 Joi 等库
4. **使用环境变量**：切勿硬编码密钥
5. **实现日志记录**：使用结构化日志（Pino、Winston）
6. **添加速率限制**：防止滥用
7. **使用 HTTPS**：生产环境中始终使用
8. **正确实现 CORS**：生产环境中不要使用 `*`
9. **使用依赖注入**：便于测试和维护
10. **编写测试**：单元测试、集成测试和 E2E 测试
11. **处理优雅关闭**：清理资源
12. **使用连接池**：用于数据库
13. **实现健康检查**：用于监控
14. **使用压缩**：减小响应大小
15. **监控性能**：使用 APM 工具

## 测试模式

请参阅 `javascript-testing-patterns` 技能以获取全面的测试指南。
