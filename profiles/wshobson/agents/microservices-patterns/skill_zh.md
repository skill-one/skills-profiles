# 微服务模式

掌握微服务架构模式，包括服务边界、服务间通信、数据管理和容错模式，用于构建分布式系统。

## 使用此技能的场景

- 将单体应用拆分为微服务
- 设计服务边界和契约
- 实现服务间通信
- 管理分布式数据和事务
- 构建容错的分布式系统
- 实现服务发现和负载均衡
- 设计事件驱动架构

## 核心概念

### 1. 服务拆分策略

**按业务能力**

- 围绕业务功能组织服务
- 每个服务拥有其领域
- 示例：OrderService、PaymentService、InventoryService

**按子域（领域驱动设计）**

- 核心领域、支持子域
- 有界上下文映射到服务
- 清晰的所有权和责任

** strangler fig 模式**

- 从单体应用逐步提取
- 新功能作为微服务
- 代理路由到旧/新系统

### 2. 通信模式

**同步（请求/响应）**

- REST API
- gRPC
- GraphQL

**异步（事件/消息）**

- 事件流（Kafka）
- 消息队列（RabbitMQ、SQS）
- 发布/订阅模式

### 3. 数据管理

**每个服务一个数据库**

- 每个服务拥有其数据
- 没有共享数据库
- 松散耦合

**Saga 模式**

- 分布式事务
- 补偿操作
- 最终一致性

### 4. 容错模式

**断路器**

- 重复错误时快速失败
- 防止级联故障

**带退避的重试**

- 暂时性故障处理
- 指数退避

**限流器**

- 隔离资源
- 限制故障影响

## 详细模式和实例

详细模式文档位于 `references/details.md`。当上层导航层不足时，请阅读该文件。
