# uv-package-manager (`wshobson/agents/uv-package-manager`)

## domain

### domain

- development

### reason

uv is a CLI tool for Python dependency management, virtual environments, and project builds.
