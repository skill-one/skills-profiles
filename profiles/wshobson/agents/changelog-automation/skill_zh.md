# 版本更新日志自动化

遵循行业标准的版本更新日志生成、发布说明和版本管理模式及工具。

## 使用此技能的场景

- 设置自动化版本更新日志生成
- 实施常规提交（Conventional Commits）
- 创建发布说明工作流
- 标准化提交信息格式
- 生成 GitHub/GitLab 发布说明
- 管理语义化版本控制

## 核心概念

### 1. 遵循 Keep a Changelog 格式

```markdown
# 版本更新日志

本项目所有重要变更都将记录在此文件中。

此格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，
本项目遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## 详细模式和工作示例

详细模式文档位于 `references/details.md`。当上层导航层级不足时，请查阅该文件。

## 概要

本次发布引入了深色模式支持，并将结账性能提升了 40%。同时包含重要的安全更新。

## 突出功能

### 🌙 深色模式

用户现在可以从设置中切换到深色模式。该偏好设置会自动保存并在设备间同步。

### ⚡ 性能

- 结账流程速度提升 40%
- 打包文件大小减少 15%

## 不兼容变更

本次发布中无不兼容变更。

## 升级指南

无需特殊步骤。标准部署流程适用。

## 已知问题

- 深色模式在初始加载时可能出现闪烁（修复计划于 v2.1.1 发布）

## 依赖更新

| 包名   | 从版本 | 到版本 | 原因                   |
| ------ | ------ | ------ | ------------------------ |
| react  | 18.2.0 | 18.3.0 | 性能改进               |
| lodash | 4.17.20 | 4.17.21 | 安全补丁               |
```

## 提交信息示例

```bash
# 带范围的特性
feat(auth): 为 Google 登录添加 OAuth2 支持

# 带问题引用的 Bug 修复
fix(checkout): 解决支付处理中的竞态条件

Closes #123

# 不兼容变更
feat(api)!: 修改用户端点响应格式

不兼容变更：用户端点现在返回 `userId` 而不是 `id`。
迁移指南：更新所有 API 消费者以使用新的字段名。

# 多段落
fix(database): 优雅处理连接超时

之前，连接超时会导致整个请求失败且不重试。此变更实现了指数退避，最多重试 3 次后才失败。

根据 p99 延迟分析，超时阈值已从 5s 提升至 10s。

Fixes #456
Reviewed-by: @alice
```

## 最佳实践

### 应做

- **遵循常规提交** - 启用自动化
- **编写清晰的提交信息** - 未来你会感谢自己
- **引用问题** - 将提交与工单关联
- **保持作用域一致性** - 定义团队规范
- **自动化发布** - 减少手动错误

### 不应做

- **不要混合变更** - 每个提交只包含一个逻辑变更
- **不要跳过验证** - 使用 commitlint
- **不要手动编辑** - 仅使用生成的版本更新日志
- **不要忘记不兼容变更** - 使用 `!` 标记或页脚说明
- **不要忽视 CI** - 在流水线中验证提交
