# bazel-build-optimization (`wshobson/agents/bazel-build-optimization`)

## domain

### domain

- development

### reason

Bazel build configuration and optimization is a core developer workflow (builds, dependencies).
