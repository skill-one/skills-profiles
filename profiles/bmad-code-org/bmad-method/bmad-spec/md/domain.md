# bmad-spec (`bmad-code-org/bmad-method/bmad-spec`)

## domain

### domain

- development

### reason

Distills raw inputs into structured software specs that downstream dev skills (architecture, UX, code) consume.
