# 迁移

迁移前，映射当前的读者、写者、数据结构、兼容性窗口和所有权。

- 定义前进路径和回滚路径。
- 保留现有数据；将破坏性步骤明确化并单独授权。
- 保持混合版本操作安全，以便发布可以重叠。
- 在适用情况下，按扩展、迁移、验证、再收缩的顺序进行。
- 使重试操作幂等，并使部分失败可观察。
- 在必要的过渡阶段验证旧路径和新路径。

在请求的阶段通过后停止；不要隐式执行后续的破坏性收缩。
