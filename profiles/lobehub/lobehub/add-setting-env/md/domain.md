# add-setting-env (`lobehub/lobehub/add-setting-env`)

## domain

### domain

- development

### reason

The skill is a code-writing workflow for adding TypeScript env-var definitions and config plumbing into an app's source tree.
