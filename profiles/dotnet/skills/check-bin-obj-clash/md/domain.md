# check-bin-obj-clash (`dotnet/skills/check-bin-obj-clash`)

## domain

### domain

- development

### reason

Diagnoses and fixes MSBuild bin/obj path collisions causing build failures — core developer build/debugging workflow.
