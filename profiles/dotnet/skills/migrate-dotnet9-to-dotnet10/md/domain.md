# migrate-dotnet9-to-dotnet10 (`dotnet/skills/migrate-dotnet9-to-dotnet10`)

## domain

### domain

- development

### reason

Upgrading a .NET project's target framework and resolving compiler, library, and dependency breaking changes is core developer-workflow work.
