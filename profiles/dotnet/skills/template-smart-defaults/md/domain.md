# template-smart-defaults (`dotnet/skills/template-smart-defaults`)

## domain

### domain

- development

### reason

Applies cross-parameter defaults to the `dotnet new` CLI during .NET project creation, a developer workflow/CLI task.
