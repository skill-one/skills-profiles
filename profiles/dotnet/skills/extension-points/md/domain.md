# extension-points (`dotnet/skills/extension-points`)

## domain

### domain

- development

### reason

MSBuild import hooks and NuGet build-extension layout are core build-system extensibility patterns.
