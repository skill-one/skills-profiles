# 二手车价格搜索

<!-- k-skill:cli-stub — 由 scripts/generate-skill-stubs.js 生成；请修改 skill.json / instruction.md 而不是此文件 -->

## 获取完整说明（必须首先执行的步骤）

运行以下命令并按照其输出作为此技能的主要说明：

```bash
npx -y @nomadamas/k-skill@0 instruct used-car-price-search
```

CLI 会检测当前运行时环境（Dolshoi vault/CloakBrowser 或通用）并仅打印适用说明，始终保持最新。CLI 随附的辅助文件列表如下：

```bash
npx -y @nomadamas/k-skill@0 files used-car-price-search
```

如果 `npx` 不可用，请安装 Node.js 18+ 或遵循 https://github.com/NomaDamas/k-skill#readme，或阅读源代码说明 https://github.com/NomaDamas/k-skill/blob/main/used-car-price-search/instruction.md。

## 即使没有 CLI 也必须遵守的硬性规则

- 未经用户事先明确批准，绝不执行支付、消息/邮件发送、最终提交、取消或公开发布。
- 绝不索取、打印或存储聊天、文件或 shell 参数中的明文凭证。
- 绝不绕过法律、物理在场、验证码、身份验证或电子签名边界。
